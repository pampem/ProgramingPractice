//AtCoder Today >> 1210
//D - Max Multiple
//Real time! (unrated)

#include<bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(){
  int N, K, D;
  cin >> N >> K >> D;
  vector<int> a(N);
  for(auto &in:a){
    cin >> in;
  }

  //K個の和を考える。
  //Dの倍数の最大値を求める。

  
  return 0;
}
